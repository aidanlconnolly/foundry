import SectionStub from "@/components/SectionStub";

export default function VcLandscapePage() {
  return (
    <SectionStub
      title="VC Landscape"
      blurb="What's happening in venture right now, by type of startup — AI concentration, the barbell market, median valuations by stage, and the most active funds."
    />
  );
}
