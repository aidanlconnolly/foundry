import SectionStub from "@/components/SectionStub";

export default function FlashcardsPage() {
  return (
    <SectionStub
      title="Flashcards"
      blurb="Spaced-repetition review of the founder vocabulary and key concepts, so the frameworks actually stick."
    />
  );
}
