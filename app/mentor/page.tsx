import SectionStub from "@/components/SectionStub";

export default function MentorPage() {
  return (
    <SectionStub
      title="AI Mentor"
      blurb="A context-aware tutor that knows where you are on the path. Explain anything, practice your pitch against a skeptical seed VC, or deep-dive any company."
    />
  );
}
