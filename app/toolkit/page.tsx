import SectionStub from "@/components/SectionStub";

export default function ToolkitPage() {
  return (
    <SectionStub
      title="Toolkit"
      blurb="Interactive calculators: the Idea Scorecard, a TAM estimator, a cap-table / dilution model, and a 'Which round am I?' diagnostic."
    />
  );
}
