import SectionStub from "@/components/SectionStub";

export default function CasesPage() {
  return (
    <SectionStub
      title="Case Studies"
      blurb="Gritty, modern teardowns of how real companies were actually built — the stack, the design, the timeline, and round-by-round money."
    />
  );
}
